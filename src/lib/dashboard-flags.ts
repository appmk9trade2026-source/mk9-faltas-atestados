/**
 * Fase 3 — Reorganização executiva do Dashboard.
 *
 * Rollback imediato: alterar para `false` restaura exatamente o layout
 * homologado nas Fases 1 e 2 (mesmos componentes, mesma ordem anterior).
 * Nenhuma query, RPC, filtro ou cálculo depende desta flag.
 */
export const dashboardLayoutV2 = true;
